var app = angular.module("TutorialApp", []);
//angular.module takes 2 arguments; first is name of module (this is for the one in html page), second is array of dependencies -> used for outside references
